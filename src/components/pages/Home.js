import React, { Fragment } from 'react';
import NavigationBar from '../layouts/NavigationBar';

const Home = () => {
  return (
    <Fragment>
      <NavigationBar />
    </Fragment>
  );
};

export default Home;
